<% if(messages && messages.length > 0) { %>
    <% messages.forEach(message => { %>
        <div class="message">
            <h1> <%=message.user%> </h1>
            <h2> <%=message.date%> </h2>
            <p> <%=message.text%> </p>
        </div>
    <% }) %>
<% } %>
